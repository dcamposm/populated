extends layout

block content
  h1= title
  
  ul
    each edition in edition_list
      li 
        a(href=edition.url) #{edition._id} #{edition.editorial.name}
        |  (#{edition.editorial.name})

    else
      li There are no books.